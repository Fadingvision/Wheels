export default function getAllBraceSymbol (id) {
    let el = document.getElementById(id),
        childs = el.childNodes,
        len = childs.length;


    for (var i = 0; i < len; i++) {
        let child = childs[i];
     }


}